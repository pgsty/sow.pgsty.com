---
title: 标签
description: "横跨文档与发布注记的主题：软件包格式、仓库模式，以及交付机制。"
---

标签描述一个页面「讲的是什么」，与它在导航中的位置无关。`rpm`、`deb`、`yum`、`apt`
是软件包格式；`plain` 与 `managed` 是两种执行模式；`publish`、`generation`、
`retention`、`transaction` 则是把仓库送到使用者手上的机制。
